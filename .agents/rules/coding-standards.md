---
trigger: always_on
---

Always use TypeScript strict mode.

No any types.

Prefer interfaces.

Prefer dependency injection.

Use async/await.

No nested callbacks.

Use descriptive variable names.

No duplicated code.

Always write reusable components.

Never create files larger than 300 lines unless necessary.