---
trigger: always_on
---

Commit format

feat:

fix:

refactor:

docs:

test:

style:

Never commit broken builds.

Run lint before commit.

Run tests before commit.

At the end of every response where code changes were made to the codebase, always output the suggested Conventional Commit message along with the exact git commands (`git add .`, `git commit -m "..."`, `git push`).