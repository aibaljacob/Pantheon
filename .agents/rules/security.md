---
trigger: always_on
---

Never expose passwords.

Never expose JWT secrets.

Never trust frontend validation.

Always validate backend inputs.

Escape user content.

Hash passwords using bcrypt.

Never return sensitive fields.